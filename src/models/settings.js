export class Settings {
    constructor(gameWidth, gameHeight, gameSpeed, gridColor) {
        this.gameWidth = gameWidth;
        this.gameHeight = gameHeight;
        this.gameSpeed = gameSpeed;
        this.gridColor = gridColor;
    }
}